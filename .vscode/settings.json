{
    "cSpell.words": ["dbeaver", "prepban", "Repliable", "Struct"],
    "plpgsqlLanguageServer.database": "roguelite",
    "plpgsqlLanguageServer.user": "roguelite",
    "plpgsqlLanguageServer.password": "roguelite",
    "plpgsqlLanguageServer.definitionFiles": [
        // Support glob.
        "**/*.sql",
        "**/*.psql",
        "**/*.pgsql"
    ],
    // The supported extention types are ['*.pgsql', '*.psql'].
    // If you want to use this extension in '*.sql', add the following settings.
    "files.associations": {
        "*.sql": "postgres"
    }
}
